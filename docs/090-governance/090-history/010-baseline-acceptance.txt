# 驗收基線

## 2026-09-10 工作地點與允許人員（本地）

來源為 `1ef21e1` 加本次工作樹；Windows、Node 24.19.0、Next.js 16.3.4、Edge 152.0.4191.66 headless、390 × 844。工作地點契約見[持久資料](../architecture/data/persistence.md#打卡工作地點)，打卡責任見[出勤](../integrations/attendance.md)。

- `scripts/pnpm.ps1 validate` 通過：工具、文件、lint、架構反例、253 modules／0 violations、Knip、型別、離線測試與建置。Web 49 tests、infrastructure 59 tests；日誌在 `.artifacts/workplaces-validate.log`，Turbo 對未變更套件有快取重用。
- PGlite 測試涵蓋管理權拒絕／撤銷、名單、停用、多個地點、最近獲准地點、範圍與精度、班中異動拒絕、版本衝突、原命令回執、事件快照，以及瀏覽器／runtime 資料庫權限。HTTP 測試核驗 LINE actor、來源、非法欄位與 4 KiB Unicode 表單。
- `scripts/pnpm.ps1 test:browser` 通過既有導覽、會員、自動打卡與新增地點管理案例。地點案例驗證新增、人員加入／移除、回應遺失後重送原 UUID／內容、換帳號阻止舊表單、管理權拒絕。證據在 `.artifacts/browser/2026-09-10T14-31-44-899Z/`，含 `workplaces.png`、`workplaces-results.json` 與 trace。
- 瀏覽器的 LINE／API 為合成替身，PGlite 為本機資料庫；未套用遠端 migration、配置真實管理名單、部署或完成手機 GPS／LINE 驗收。既有出勤、Coin 與通知回歸不等於正式地點已啟用。

## 2026-09-10 Next.js 站內導覽與會員綁定狀態（本地）

來源為 `be35537` 加本次工作樹，Windows、Node 24.19.0、Next.js 16.3.4、Edge 152.0.4191.66 headless、390 × 844。保留同工作樹的 profile／telemetry 修改，以整合後版本驗證。

一般站內連結使用 Next.js Link；外部瀏覽器開啟 Google 關聯使用 router.push，取消與確認成功使用 replace。會員模式由 URL 決定，模式切換重建確認生命週期；刪除重複 linking state 與註冊／恢復頁用不到的 Google 提示分支。請求取消及確認一致性規則見 [Supabase 接線](../integrations/supabase.md#line-內外瀏覽器接續)。沒有新增依賴、業務快取或通用狀態層。

- `scripts/pnpm.ps1 test:browser` 通過正式建置、既有任務／公告／入口回歸與九組會員案例。五條站內連結均只有一份 HTML document、一次 LIFF 初始化；Google 模式進出、換帳號與成功接續不新增 document。案例中的明確重新整理新增一份 document，並非回歸。
- 會員案例另涵蓋 profile 延遲／失敗不阻塞查詢、過期 profile 回應作廢、取消後無確認暫存、返回時中止在途 GET、前進／重新整理、登出失敗恢復，以及 session 改變時先拒絕舊確認、重新核對後只送出一次 POST。沒有 browser pageerror。
- 首次新增測試揭露 Supabase 登出失敗也可能清除本機 session，已修正為重新核對 session 再決定選帳入口；測試同時驗證錯誤提示。Next.js route announcer 的空白 alert 與產品錯誤分開斷言，未移除產品錯誤檢查。
- 瀏覽器證據位於忽略的 `.artifacts/browser/2026-09-10T12-22-43-224Z/`，包含 `report.json`、`membership-results.json`、截圖與 trace。執行方式見 [瀏覽器操作文件](../../scripts/browser/README.md)。
- 完整 `scripts/pnpm.ps1 validate` exit 0，設定 TURBO_FORCE=true，型別／測試／建置均無 Turbo 快取命中。172 項 Node 測試、lint 341 files、架構 245 modules／0 violations、Knip 及六套件建置通過；紀錄為忽略的 `.artifacts/navigation-modernization-validate.log`。

LIFF、Google session、登出 API 與業務 API 使用合成替身，其他遠端請求阻擋。本地結果不代表真實 Google OAuth、正式會員資料庫、LINE Bot 發送或 Android／iOS 驗收；本次沒有部署或發送外部訊息。

## 2026-09-10 會員載入與主要頁面遙測

來源：`be355379` 加工作樹修改，Windows、Node 24.19.0、Next.js 16.3.4、Edge 152.0.4191.66，390 × 844。本批只調整會員 profile 等待與遙測白名單，保留同 checkout 既有 Google 登入與導航修改。

[會員初始化](../../apps/web/src/modules/membership/use-membership.ts) 取得 LINE token 後並行啟動顯示用 profile 與會員請求，會員操作不等待 profile；沿用 AbortSignal 拒絕過期 profile 回應。主要頁面加入既有 [遙測白名單](../../apps/web/src/shared/browser/telemetry.ts)，仍剝除 query、hash 與 URL 憑證，拒絕任務／公告 ID 詳情路徑。

`scripts/pnpm.ps1 validate` 通過：架構 245 modules、0 violations，Web 型別、48 個 Web 測試與 Web build 實際重跑；其餘未變動套件部分使用 Turbo 快取。`scripts/pnpm.ps1 test:browser` 另以隔離正式建置通過既有導航及 9 組會員案例，包括 profile 一直未完成、profile 失敗、重新整理後過期 profile 不顯示。執行報告與合成資料截圖位於 `.artifacts/browser/2026-09-10T12-23-54-532Z/`，測試來源為 [會員瀏覽器測試](../../scripts/browser/check-membership.mjs)。

未新增套件或平行路由；未部署、未發布 Rich Menu，未量測正式 LCP／INP、LINE 真機或確認正式遙測收件。本結果不代表任務分頁、資料庫鎖與建置快取調整已實施。

## 2026-09-10 MINI App 入口與初始載入修正

來源：`78f5c0c` 加本次未提交修改，Windows、Node 24.19.0、Next.js 16.3.4、本機 production server 與 Edge headless。既有 Google adapter／Knip 工作樹修改保留，不計為本次成果。

`/expenses` 原本直接掛載支出面板，違反[入口契約](../architecture/web/entry-and-access.md)；現先由既有 EntryResolver 完成 LIFF 接續及意圖解析，無支出回工作台，合法支出才掛載面板。移除只轉接畫面的 `expenses/content.tsx`，由 page 直接組裝。另補上公告頁缺少的 MiniAppRuntime，查詢等待 SDK 就緒，修復重新整理後初始化失敗。

Supabase 瀏覽器 SDK 改為需要 Google 登入時才動態載入；仍使用單一 client、PKCE、原有 token 過濾儲存及後端資格核驗。沒有新增依賴或業務快取。[量測腳本](../../scripts/browser/measure-loading.mjs) 在相同 LINE／API 替身條件下得到：

| 路徑 | 延後載入前 JS bytes | 延後載入後 JS bytes | 減少 |
| --- | ---: | ---: | ---: |
| `/home` | 475450 | 475450 | 0 |
| `/attendance` | 729363 | 490824 | 32.7% |
| `/me` | 729748 | 491220 | 32.7% |
| `/expenses?tasks=1` 接續 `/tasks` | 1222484 | 983945 | 19.5% |

以上為解壓後 script 大小。舊入口接續仍有兩次 document，攔截路由停用 HTTP 快取，重複資源累計；不把此值當正式壓縮傳輸量或手機速度。比較前已套用入口修正，比較後另含公告 SDK 接線修正；兩者公告以外路徑的主要差異為延後 Google SDK 載入。

依使用者指定 GitHub 搜尋，透過公開 API 檢視最新候選：交通資訊 [motc-mini-app](https://github.com/tobytoy/motc-mini-app) 使用 Vite／Leaflet；收據 [smartslip-mini](https://github.com/suppachai0/smartslip-mini/blob/main/lib/useLiff.ts) 使用 Next.js 與完整 LIFF import；估價 [luya-paint-estimator](https://github.com/devme3me-cell/luya-paint-estimator) 使用 Vite，初始化載入 profile 與平台能力。已讀的 manifest／入口／設定没有同條件效能基準，不能據此排名最快，也不構成本專案換框架的證據。[官方 TypeScript 範本](https://github.com/line/create-liff-app) 作初始化參考；[Pluggable SDK](https://developers.line.biz/en/docs/liff/pluggable-sdk/) 僅適用 npm 版本，官方縮減上限不是本專案實測，本次保留 CDN SDK。後續若改 SDK，須另驗證實際 bundle 及 LINE 手機接續。

完整 `scripts/pnpm.ps1 validate` 通過，涵蓋 lint、245 modules／0 架構違規、Knip、型別、離線測試及 production build；未變動套件部分使用 Turbo 快取。Edge 導覽腳本通過入口、公告／任務導覽與命令重試、團隊成員管理，LINE 與 API 使用替身。操作方式見[瀏覽器測試](../../scripts/browser/README.md)。

未部署、未修改 LINE endpoint／選單、未完成真實 Google OAuth 或 Android／iOS 驗收。LINE endpoint 仍指向 `/expenses` 是症狀推論，本次未讀回 Console 設定。正式 LCP／INP／CLS、Lighthouse 與真實 LINE 登入未量測；依 [LINE 效能指引](https://developers.line.biz/en/docs/line-mini-app/develop/performance-guidelines/) 在正式環境排除登入頁另行驗收，不宣稱已達最高效能。

## 2026-09-09 舊團隊內容下架

目前團隊入口為最新消息、合作夥伴、夥伴推薦，內容來源未提供時明示尚未開放。原三項畫面、專用資料型別、讀寫分支與雛型已移除，舊網址意圖不再接續；保留任務所需的團隊成員管理。一般公告停止讀取舊團隊範圍內容。

向前清理檔為 `20260909123932_retire_team_content.sql`，移除舊內容專用表、團隊範圍公告及其版本與舊命令回執，保留共用成員、任務、一般公告及任務稽核。下方早期功能紀錄僅作歷史證據，不代表目前仍提供舊功能。

本地完整 `scripts/pnpm.ps1 validate` 通過，含型別、lint、架構、Knip、測試及 production build；資料層 50 項、application 18 項及 Web 45 項測試通過。清理遷移以 PGlite 載入有舊內容的資料集，確認專用表不存在、舊資料清除及共用資料保留；另驗證清理前的資料庫也不會透過新版一般公告頁讀取舊內容。Edge headless 390 × 844 的既有導航腳本通過新三項入口、舊控制不存在、成員管理及公告／任務回歸；LINE SDK 與 API 使用合成替身，沒有正式寫入。

使用者授權發布後，程式提交 `372381a3ecf2772e8bd390aefde77c6c445a0659` 已推送至 GitHub `963sup/line-work-assistant` 的 `codex/release-20260909` 分支。Vercel 正式專案 `line-bot-963sup` 部署 `dpl_3dHWjy5G4aVyW5DH61Hy9AHPb1yu` 為 READY，正式網域 `https://line-bot-963sup.vercel.app` 已讀回相同 deployment ID；本次未合併 main。

先確認新版 `/team` 與三個 `/planned?feature=...` 頁面回覆 200、包含新標題且沒有舊三項文案，再由既有 CLI 遷移流程在當時的 Supabase production environment 套用唯一待執行的 `20260909123932`。migration history 已讀回相同版本；`team_handovers`、`team_meetings`、`team_meeting_participants` 均不存在，`announcements_global_only` 與 `team_membership_commands_only` 均 validated。操作前舊內容全部為 0 筆；操作後會員維持 1 人，共用團隊、成員關係、任務、任務事件與一般公告維持 0 筆。

遷移後 `/api/health`、`/team` 回覆 200；未登入的 `/api/team`、`/api/tasks`、`/api/announcements` 回覆 401。Vercel 連接器曾回覆 403，改以既有已登入 CLI 部署；完整正式環境變數下載被自動審核拒絕，未下載，亦未以其他方式讀取其秘密。本次未發布 LINE 實體圖文選單，未做真實會員寫入或手機驗收。

## 2026-09-09 公告第一版本地實作

來源：HEAD `f028b5d` 加公告工作樹；Windows 本地，未提交或部署。`/notifications` 已由雛型改接公告 API、application 用例、domain 規則與 PostgreSQL repository。公告資料使用 `20260908164718_announcements.sql`，目前 repository 的全體／團隊隔離也依賴後續 team_sources migration 的 group_id；應按完整 migration 序列驗證，不能只套單一檔案推定可用。

公告定向測試 5 項通過：PGlite 執行 migration 與實際 SQL，涵蓋草稿／發布／更正／撤回、有效期間、分頁、會員及管理權、同操作重送、預期版本衝突、版本寫入失敗回滾、全體入口拒絕團隊公告、前端資料投影，以及 HTTP 身分／來源／JSON 大小限制與入口接續。PGlite 的並行呼叫測試不等於遠端 PostgreSQL 多連線壓力驗收。

最終完整 `scripts/pnpm.ps1 validate` 通過：工具、文件、lint、架構合法／違規反例、變更計畫反例、架構（226 modules，0 violations）、Knip、型別、測試及 build。Web 37 項測試、infrastructure 48 項測試通過，另有 application 17 項與 agents 4 項；部分套件命中 Turbo 快取，Web production build 重新執行。先前其他團隊功能造成的型別與 ports 邊界阻塞已於此輪消除，沒有放寬規則或跳過檢查。

尚未套用遠端 migration、配置真實公告管理者、发布 LINE 選單或部署 Web。既有本地 LINE 登入初始化失敗仍未解除，正式會員瀏覽器／手機操作未驗收。人工稽核確認只記錄發布責任，不自動讀取、遮蔽或核驗原案；沒有 LINE 推播。

## 2026-09-09 任務首版本地實施

來源為本次未提交工作樹，Windows、Node 24.19.0。任務三入口使用同一 `/tasks`，新增 `/api/tasks`、application 用例、domain 狀態規則與 PostgreSQL adapter；migration `20260908165129_tasks.sql` 相依於同工作樹的團隊 migration，共用 WorkGroup，不重建團隊資料。流程及限制由[任務事項](../product/tasks.md)擁有。

- 任務及受影響回歸共 11 個測試通過：SQL 流程／隔離／回滾及既有會員測試 3 個，HTTP／入口接續 7 個，用例拒絕 1 個。SQL 使用 PGlite 隔離庫，沒有遠端寫入；不宣稱已測實際 PostgreSQL 多連線鎖競爭。
- 架構反例 3 個測試通過，包含新增任務對會員公開 composition 的合法引用及私有引用拒絕。
- Next.js 正式 build 通過（本輪 Web 重新建置）；本地 `/tasks` HTTP 200，無 token 的 `/api/tasks` HTTP 401。這不代表登入後 UI 或手機端到端通過。
- 全庫 validate 在同時新增的團隊檔案格式檢查失敗；曾另見團隊 port 匯入架構違規及尚未建立 panel 的 Knip 錯誤。這些是當時共享工作樹結果，不能宣稱全庫通過。既有固定 RLS 表數檢查改為逐表要求 RLS 後，其回歸通過。

尚未執行遠端 migration、部署、LINE 選單發布、兩位真實會員或 Android／iOS 驗收。圖片保留規劃中；正式環境放行仍屬 RM2。待確認命令只在當前瀏覽器頁籤保存，無 AI、推播、附件或 Google 任務寫入。

## 2026-09-09 公告雛型歸入 Web 模組

來源：本次未提交工作樹，Windows 本地環境。`/notifications` 由既有 AppShell 組裝 `modules/announcements/announcement-prototype.tsx`，專用樣式同置該模組。三分類、明細、新增與撤回僅操作記憶體中的示例資料，重新整理重置；不包含正式發布、保存、權限管理、核准或推播。LINE 選單入口未調整。

`scripts/pnpm.ps1 validate` 通過：工具、文件、lint、架構反例、變更計畫反例、架構（200 modules，0 violations）、Knip、型別、離線測試與 build。Web 型別、測試（31 項）及 build 重新執行，其餘套件部分命中 Turbo 快取。

本地 production server 的 `/notifications` 已進入既有 LINE 登入與會員資格檢查；瀏覽器顯示 LINE 登入元件初始化失敗，未取得會員登入，因此整合後的公告互動尚未完成瀏覽器驗收。先前獨立 HTML 的互動驗證不代替此結果。未部署、未驗收 LINE 真機或正式資料流程。

## 使用本文件

2026-09-07 曾只重整目標文件，當時未讀程式、未執行產品或遠端驗收；該次整理不代表後續個別驗收。舊紀錄中的「目前」「已完成」「待驗收」只適用該次版本，不是線上現況；較新結果也不能自動覆蓋未驗證的範圍。

## 2026-09-09 異常通報原生表單

來源為既有 HEAD 加本次 incident URI／素材工作樹；環境為 Windows 登入中的 Chrome、Google Forms／Drive 與 LINE Messaging API。依使用者要求沿用日誌方式，三份表單各建立八項必填業務題目並連結原生回覆試算表，分別保存於對應的 Google Forms 與試算表資料夾。改善處理填寫原異常識別資訊，交由人工核對；額外 Apps Script 審核／通知草稿未授權執行、未啟用。

正式短網址、題目與三份空白必填攔截均確認；未提交回覆，尚未驗證實際回覆寫入。圖片已移除三個「規劃中」：2026-09-08 incident menu 由 `richmenu-8666bd31279f3bd0e754a6024ff87801` 更新為 `richmenu-567c9c2c019e2dbd5d59cb3a6bf0f0f5`，2026-09-09 再更新為 `richmenu-4bdb6eac9e1d5ea2b03f405c9d3ec897`（1536 × 1024 JPEG，161948 bytes）。LINE 定義、圖片雜湊、alias 及一位已知 active 會員個人綁定讀回通過，default 未變；回執為 `.artifacts/incident-before.json`、`incident-activation.json`、`rich-menu-incident-created.json`。此結果不涵蓋手機操作、未登記好友、自動審核或寄信。

推送前依第一性原理核對：三個入口直接使用原生 URI，未附加 LINE 身分或 token；Google Forms／Sheets 承接填寫及保存，不新增後端 API 或資料模型。工作樹完整 `validate` 通過，型別、測試與 build 使用 Turbo 快取；此檢查不替代實際提交及手機驗收。

## 2026-09-08 選單功能實施盤點

來源 HEAD `d489557303c1a159f4ce586db36cfebd17de85b8` 加本次文件工作樹；環境為本地 Windows，範圍是八張 JPEG、選單定義、入口接續及對應程式唯讀盤點。未查詢 LINE／Vercel／資料庫、未提交表單、未重跑產品測試；下表是本地實施狀態，不是線上可用清單。歷史發布證據仍只代表原紀錄日期及版本。

證據：

- [選單定義](../../apps/web/src/modules/assistant/rich-menu/definition.ts)列出八頁與全部入口；[接續映射](../../apps/web/src/shared/presentation/entry-destination.ts)決定路由。
- [planned](<../../apps/web/src/app/(app)/planned/page.tsx>)、[tasks](<../../apps/web/src/app/(app)/tasks/page.tsx>)、[notifications](<../../apps/web/src/app/(app)/notifications/page.tsx>)、[feedback](<../../apps/web/src/app/(app)/feedback/page.tsx>)明示未開放。僅有入口，不構成對應業務實施。
- [出勤畫面](../../apps/web/src/modules/attendance/panel.tsx)、[命令處理](../../apps/web/src/modules/attendance/command.server.ts)、[出勤用例](../../packages/application/src/attendance/clock.ts)已有上／下班接線；本次未逐條重新驗證交易與通知。
- [會員 API](../../apps/web/src/modules/membership/api.server.ts)、[會員畫面](../../apps/web/src/modules/membership/panel.tsx)與[表單來源](../../apps/web/src/modules/diary/form.ts)已存在；外部表單權限與提交效果本次未核驗。

| 功能 | 本次查得的實施狀態 | 缺口 |
| --- | --- | --- |
| 主選單／返回 | 已有分類切換、會員 URI、出勤意圖及返回 postback 定義；本次未重跑同步 | RM1 |
| 上班 | 已有 UI、命令處理與 application clockIn；未重跑業務測試 | RM1 |
| 下班 | 已有 UI、命令處理與 application clockOut；未重跑業務測試 | RM1 |
| 會員中心 | 已有會員讀寫 API、畫面與 /me 接續；未重跑身分流程 | RM1、S1 |
| 任務看板 | 入口至 /planned；明示不提交或保存資料 | RM2 |
| 我的任務 | 入口至 /tasks；頁面明示未開放 | RM2 |
| 發布任務 | 入口至 /planned；明示不提交或保存資料 | RM2 |
| 團隊公告 | 入口至 /notifications；頁面明示未開放 | RM3 |
| 交接事項 | 入口至 /planned；明示不提交或保存資料 | RM4 |
| 會議紀錄 | 入口至 /planned；明示不提交或保存資料 | RM4 |
| 工作日誌 | 已有固定外部表單 URI；本次未開啟或提交表單 | RM5 |
| 費用申請 | 入口至 /planned；明示不提交或保存資料 | RM6 |
| 請假申請 | 入口至 /planned；明示不提交或保存資料 | RM7 |
| 最新公告 | 入口至 /notifications；頁面明示未開放 | RM3 |
| 稽核公示 | 入口至 /planned；明示不提交或保存資料 | RM8 |
| 歷史公告 | 入口至 /planned；明示不提交或保存資料 | RM3 |
| 現場稽核 | 入口至 /planned；明示不提交或保存資料 | RM8 |
| 異常回報 | 入口至 /feedback；明示不接收或儲存回饋 | RM8 |
| 改善處理 | 入口至 /planned；明示不提交或保存資料 | RM8 |

逐項設計見[功能設計](../product/menu-feature-design.md)，缺口唯一來源見[RM 清單](../architecture/governance/design-gaps.md#選單逐項功能缺口)。14 個規劃中子功能皆尚未由該選單提供可用業務流程；工作日誌是外部入口，上／下班及會員有程式不表示本輪真機通過。費用申請不能引用既有收據 confirmed 狀態作已核准證據。

## 2026-09-08 四類子選單改為三項

來源 `a1ad855`。依使用者指定順序，異常通報為現場稽核／異常回報／改善處理，公告通知為最新公告／稽核公示／歷史公告，表單作業為工作日誌／費用申請／請假申請，任務事項為任務看板／我的任務／發布任務。四張圖片與點擊區域同步改為三張橫卡；保留返回主選單。沒有新增業務 API 或資料模型；當日素材與提示詞僅能由 Git 版本追溯。

- 完整 validate exit 0，包含幾何、型別、測試及 production build；工作日誌沿用表單連結，其餘保持原有未開放語意與規劃中標示。
- GitHub main 已推送；Vercel production `dpl_6wk3XBVaUPoutcKJjHm85gdLhyHQ` 回讀 Ready，正式站 11 項 HTTP 檢查通過。
- LINE 驗證、建立及上傳四張新圖：incident `richmenu-8666bd31279f3bd0e754a6024ff87801`；announcements `richmenu-3f26f4915d1c8223afa9bd1024c721dd`；forms `richmenu-8ed033e0c81f10473da7a948af570994`；tasks `richmenu-18f2bc37cbaf59850b166671a73e44e7`。更新固定 aliases，另外回讀 34 個歷史 aliases 導向目前選單；未新建版本名称。
- 啟用回傳 `knownActiveMembers: 1, verified: 1`；八張 definition、alias、圖片 SHA-256 與 default 全部回讀一致。主選單與上下班圖片／ID 未變；手機點擊與顯示尚未實機觀察，不能將 API 結果當手機驗收。

證據保存於忽略的 `.artifacts/submenus-validate.log`、`submenus-deploy.log`、`submenus-preview.json` 與 `submenus-before/`；未送訊息或寫入出勤。

## 2026-09-08 個人選單實際更新與固定入口

來源 `e28c8d1`。查核正式會員身分與 LINE 個人選單，唯一已知有效會員仍綁定最早的 `richmenu-b025a1adcebf4298b285a767299bbad1`；個人設定蓋過 default，且不在先前批次對應內。已直接更新並回讀成功，不再等待舊批次完成來判定該會員是否更新。

- 發布固定使用 `work-assistant-${page}` aliases，既有 alias 直接更新；啟用流程從正式有效會員取得目標，依出勤權威狀態逐一連結並 GET 回讀。worker 亦須回讀相符才完成 outbox，失敗不算成功。
- 完整 validate exit 0；新增固定 alias、alias 更新／個人選單讀取與有效會員篩選測試。來源已推送 GitHub main，Vercel production `dpl_CkpFURUgu6iz1BYV49Vv2LJ2temi` 回讀 Ready，正式別名為 https://line-bot-963sup.vercel.app。
- 啟用回傳 `knownActiveMembers: 1, verified: 1`。正式 worker 再次實際同步此會員，回傳 `synced: 1, notified: 0, failed: 0`；LINE GET 確認個人選單為新上班圖 `richmenu-6fbd54e83e8797f4ffef9c5bbaf95769`。未新增出勤紀錄或發送訊息。
- 新 default 為 `richmenu-5e5d1a3cc2b3067743a2127e03c33615`，下班為 `richmenu-66970d2ee22bd4ddd5eda7490b3399d2`。八份 definition、固定 alias、圖片 SHA-256 與 default 全部遠端回讀相符；會員中心 URI 直接進 MINI App。另將 34 個既有歷史 aliases 導向現有選單並回讀，沒有新增版本化名稱。
- 正式站 11 項 HTTP 檢查通過，包含會員中心、已移除群組路由、出勤授權及 worker。LINE follower 清單 API 回 403，因此只聲明已知有效會員的個人綁定完成，不聲明已枚舉全部未註冊追蹤者；也不以 API 回讀代替實際手機畫面觀察。

本地證據位於忽略的 `.artifacts/personal-menu-validate.log`、`personal-menu-deploy.log`、`verify-production-personal-menu.mjs` 與 `existing-menu-alias-repair.json`。個人身分清單不納入 Git。

## 2026-09-08 群組入口清除與會員中心直達

來源 `5260373`。移除未開放的 `/me/groups`、`/groups`、`/group` 頁面、入口意圖與導覽；同步契約、架構白名單及測試，沒有群組列表 API 或資料表需要刪除。LINE 群聊事件與既有授權維持原責任。

- 主圖會員中心改為 `https://miniapp.line.me/2011311656-f1TGj0p4?membership=1`，LINE 初始化後接續 `/me`；移除 member 子選單定義與 JPEG。保留會員資料、Coin 與 Google 關聯行為，頁面改為淺綠圓角卡片；沒有新增會員 API 或改會員規則。
- 團隊圖片使用內建 imagegen，參照綠色主圖，重排為團隊公告、交接事項、會議紀錄三卡，全部保留規劃中提示。素材 `scripts/rich-menu/assets/work-assistant-team.jpg` 為 1536 × 1024、160008 bytes；提示詞僅由當日 Git 版本追溯。
- 完整 validate exit 0。原相容入口反例仍檢查缺頁、錯誤位置與不合法內容；移除兩條群組相容路由後，必要入口數由七改五。產品來源、入口測試及架構檢查已無群組列表引用。
- 本地 production server 加隔離 LINE／會員 API，Edge 390／768px 驗證 6 情境、19 項檢查通過：active 畫面、Coin 簽到、取消暫停不寫入、刷新、pending／停權／未註冊／服務失敗提示、無橫向溢出與 pageerror；三個已刪網址皆 404。這不代表真實 Google 或 LINE 手機驗收。
- GitHub main 已推送；Vercel production `dpl_BZ1ThQH8QbiGJqRsjV3aEfvXiaJt` 回讀 Ready。正式站 11 項 HTTP 檢查通過，包括 `/me` 200、三個群組網址 404、出勤授權拒絕及受保護 worker 200。
- 八張 LINE v4 定義、aliases、圖片 SHA-256 與 default 全部回讀相符。主選單 `richmenu-9de4dec398622fc517f0282cdbbbc896`，上班 `richmenu-183be210444d72afc98676645314ffdb`，下班 `richmenu-91026f4ce104a6dce187e52fbd83f7d7`。會員中心為直接 URI，團隊選單已無群組入口。
- 已驗證並提交 v2／v3 共 18 個已知舊選單到 v4 的原生 batch link；舊 member 子頁回到新版 home，其餘保持原分類與出勤狀態。使用固定 resumeRequestKey，request ID `12564ade-d900-4b45-b588-8fb8ed721995`；LINE 回 202，進度仍 ongoing，不宣稱全部個人連結已切換。完整對應與回執保存於忽略的 `.artifacts/membership-v4-menu-replacement*.json`，沒有群發訊息或刪除舊遠端選單。

本地證據位於忽略的 `.artifacts/membership-direct-validate.log`、`membership-direct-browser.mjs`、`membership-direct-*.png`。API、資料模型、出勤規則及地點設定未在本次改動。

## 2026-09-08 中央上／下班環狀主選單

來源 `5600c8e`，依使用者參考圖改為中央直接上／下班及外圈六分類。兩張圖皆使用內建 imagegen，1536 × 1024 JPEG，上班 183101 bytes、下班 225655 bytes；home 共用上班圖。主提示由當日 Git 版本追溯。

- 完整 validate exit 0；幾何測試核對七區不重疊、中央點直達 URI、兩狀態外圈位置與分類相同、六子頁返回使用權威狀態 postback、登入接續保留明確操作。未改 API、資料模型或打卡規則；中央點擊先進 MINI App，定位及本人確認後才寫入。
- 已推送 GitHub main，Vercel production `dpl_4xXo9VB7xopz2Csm9jNEFq3TXMA5` 回讀 Ready，正式站 7 項 HTTP 檢查通過。
- 九份 v3 定義經 LINE validate、建立與圖片上傳；aliases 在部署前備妥，完成部署再啟用 default。遠端九張 definition／alias／圖片 SHA-256 及 default 回讀皆符本地。預設 `richmenu-64b47344e938f9deabd9d4bdba6632ac`、上班 `richmenu-edf495f98cb0523598e7bb693306a6b0`、下班 `richmenu-eac46adc3e1b6866ca964e30cc965e65`。

未批次更改舊個人連結，舊選單可經原出勤入口重讀狀態換新版；不以更新 default 宣稱所有既有手機已即時切換。真實手機點擊、定位打卡仍待驗收。證據位於忽略的 `.artifacts/attendance-ring-validate.log`、`attendance-ring-create.log`、`attendance-ring-deploy.log`；舊 v2 回執保存於 `.artifacts/rich-menu-v2`。

## 2026-09-08 兩操作出勤與正式 Supabase

實作來源為 `0b2d4f2`。依使用者確認無舊出勤，移除七命令／分段交接，改為上班、下班兩命令及真實起訖 session；跨日和時段分類自動計算，不自動打卡、不扣假設休息、不將分類當法定加班。

- 完整 `scripts/pnpm.ps1 validate` exit 0：tooling、文件、lint、架構、Knip、型別、測試與 production build 通過。套件／Web runtime 測試為 agents 4、application 14、infrastructure 32、Web 31 與 env 3；未修改套件可使用 Turbo 快取。
- 本地 production server 加隔離 LIFF／API／定位替身，Edge headless 390 × 844 的 5 個情境、24 項檢查通過：上／下班跨日、回應不完整使用同 UUID 重送、過時操作意圖、定位拒絕、登入身分切換清除資料。無 pageerror 或水平溢出。這不是真實 LINE 手機驗收。
- 遠端確認為當時的 Supabase production environment，舊出勤表為空，已套用 `20260907175144_attendance_sessions.sql`；出勤 7 表啟用 RLS、舊 segments 已移除，會員與 Coin 未清空。第一次套用因 LOCK 缺交易遭拒，核對未部分生效後加上明確交易再套用成功，未修改已套用檔案。
- 正式執行角色測出 Auth schema 42501。`20260907183500` 的 USAGE grant 雖 CLI 成功，遠端權限回讀仍 false：託管 schema 的 postgres 沒有轉授權限。以 `20260907183724_attendance_account_status.sql` 建立僅 boolean 的受限函式修正；保留前次 migration history，不以 CLI exit 0 代替權限驗收。
- 使用真實 runtime 連線及 line_app 角色，在單一交易內驗證上班／下班／UUID 重送、通知同交易、兩種 outbox 領取、原始起訖不可改寫、事件 UPDATE 拒絕；合成會員及出勤全部 rollback 並回讀確認未殘留，未向 LINE 發送測試訊息。
- Supabase pg_cron／pg_net 已啟用，Vault 保存 worker endpoint 與 secret；固定 `attendance-maintenance` job 每分鐘執行。cron.job_run_details 已回讀 succeeded；無待辦時 0 rows，不產生 HTTP。排程定義位於 scripts/attendance/schedule.sql，不由 Web 啟動執行 DDL。
- `0b2d4f2` 已推送 GitHub main；Vercel production `dpl_JAzfxW5QxPKCR8SoekM8DHZ4C3d3` 回讀 Ready，正式別名為 https://line-bot-963sup.vercel.app。7 項 HTTP 檢查通過：頁面 200、匿名讀取／操作 401、舊加班 endpoint 404、worker 錯誤憑證 401／有效憑證 200。Supabase pg_net 實際呼叫同一正式 worker 亦回 200，零待辦、無逾時；不代表曾真實投遞出勤訊息。
- 九張新版 LINE 圖片已建立／上傳及啟用 v2 aliases，逐張遠端回讀 definition、alias、圖片 SHA-256 全符本地。預設 home 為 `richmenu-881c10f9b5c9dae526f9b7bc6f252fb9`，上班圖 `richmenu-ada25d26a7fbc278757153ef029151d1`，下班圖 `richmenu-1e6faeb5764c22148ef9bd7f9ac4bd0e`；兩張 JPEG 皆 1536 × 1024。未批次更改既有個人連結，會員開啟出勤入口時依權威狀態更新。

本地證據位於忽略的 `.artifacts/attendance-final-validate.log`、`attendance-browser.mjs`、`attendance-*-390.png`、`attendance-live-rollback.mjs`；環境密鑰不納入 Git。工作地點與半徑尚未提供，真實定位打卡仍拒絕寫入；LINE 手機切換、實際訊息送達與完整法定出勤／薪資驗收仍待完成。

## 2026-09-08 Web 責任遷移驗收

來源為 `a2dafbad30dc5e5030321946340fd1315f62ebfd` 加本次工作樹。先固定 owner 與不變契約，再搬移程式；現行目錄邊界見[目錄與依賴](../architecture/platform/monorepo.md)。未修改 packages 業務實作、schema、公共 URL 或外部服務設定。

- 39 個既有檔案搬移；排除 imports 並以 TypeScript printer 正規化後，33 個主體一致，其餘為四個 panel 移除外框、收據懶載入入口及會員 HTTP 分離。新增五個檔案承接必要責任；Web TS／TSX 合計 111 個，分別為 app 64、modules 34、shared 13，舊 features／components／server 已清空並移除。
- Web 原始碼快照 SHA-256：`71e8e357a6d09d4fbfd01da80e0e6c05fe9a7577c956981a8c949a1dfed9ae82`。以完整來源路徑排序，逐一串接 repository 相對路徑、NUL、檔案原始 bytes、NUL；包含 src 下全部 TS／TSX。
- `scripts/pnpm.ps1 validate` 最終 exit 0：tooling、docs、lint、architecture:test、change:plan:test、architecture、Knip、typecheck、test、build 通過。架構圖 193 modules／0 violations；反例涵蓋新 modules／shared 邊界、私有跨模組引用及間接／type-only server 引用。
- 本次 Web typecheck、test、build 有重新執行；Web 測試為 31 項及環境載入 3 項，全部通過。未變更的核心套件使用 Turbo 快取，不宣稱底層全部重新測試。
- HTTP 新增有限串流的 2KB／8KB byte 邊界、超限取消與讀取失敗傳遞；收據 API 超限維持 413／原文案且不改資料。既有 owner、資格、出勤版本／重送、Webhook 與 worker 離線案例通過。
- 正式 build manifest 的 53 個產品 page／API URL，與基準 commit 的路由來源集合完全一致；框架自動產生的內部路由不算產品 URL。
- 本地 Next 正式模式在 `127.0.0.1:3187` 執行 9 項 HTTP 檢查：privacy、home、attendance、register、舊 membership、expenses 的回應及入口文案正確；membership、attendance、expense 匿名 GET 均 401。使用無憑證子程序環境與 `VERCEL=1` 阻止載入根秘密，測完關閉程序。這是 SSR／HTTP 檢查，沒有執行登入後瀏覽器互動。

本地證據位於忽略的 `.artifacts/web-migration-validate.log`、`web-migration-smoke.json`、`web-migration-source.json`；不作產品 runtime 依賴。文件更新後另跑 docs:check 與 git diff --check。主代理自行核對，不稱獨立審查。

限制：未部署、未改 LINE 選單或發訊、未做 LINE／Google／資料庫遠端驗收；登入後 UI、取消／返回、session 切換與 Android／iOS 實測仍列 [AR2](../architecture/governance/design-gaps.md)。未量測效能、令牌或費用節省。出勤持久投遞流程保持原有語意，本次不合併流程。

## 2026-09-07 Codex Desktop 專案配置驗收

以 commit `e6a1cf90b15057a48c092b8f7273137705728798` 加當時未提交工作樹核對。本次只修正代理上限、配置檢查與其反例、開發入口及載入說明；依賴鎖定、runtime 工具與其他產品文件修改由其他工作進行，不歸為本次成果。

- `.codex/config.toml` 原始檔 SHA-256 為 `94460566cdd2f81c69d72df795c3529df1b97ca54d8f3e8d406f23f73fa4eee9`。上限改為 `[agents].max_concurrent_threads_per_session = 5`，不含主代理；移除原本錯位的 feature 區段。
- Windows CLI `0.153.4` 啟動獨立 `app-server --strict-config --listen stdio://`，經 initialize、config/read（指定專案 cwd、includeLayers）讀回 enabled=true、上限=5，兩者來源皆為專案層且沒有 disabledReason；模型預設仍來自使用者層。未建立 thread／turn、未呼叫外部業務工具；診斷程序已關閉。
- 同一 App Server 的 skills/list 讀回 22 個專案 skills 全部 enabled=true、errors=0。三個專案角色在本工作階段工具目錄可見，唯讀欄位通過靜態檢查；未為驗收另行委派，角色執行行為與上限壓力測試未驗證。
- tooling:check 通過（當時共 16 個測試，含本次新增的 4 組代理配置反例／正例）；直接重播修改前 TOML 被拒絕，指出上限錯放在 features.multi_agent_v2，修改後通過。tooling:rules 由原生 execpolicy 通過，未執行案例命令。
- 完整 validate 的 tooling、docs、lint、architecture:test、change:plan:test 與 architecture 通過，停在 deadcode：其他工作新加入的 smol-toml、yaml 尚未被引用；此次完整 validate 為 exit 1，不標為全通過。另行 typecheck、test、build 均 exit 0，全部使用 Turbo 快取，未宣稱重新執行產品測試。

這是本地配置與載入證據，不代表已開啟的 Desktop 任務已更新設定、Windows sandbox／MCP 全面隔離、遠端部署或 LINE／Google 業務驗收。後續工作樹與依賴變動後，須重新跑受影響檢查；未量測令牌或效率節省。

設計主契約見 [入口](../architecture/web/entry-and-access.md)、[目錄](../architecture/platform/monorepo.md)、[畫面](../product/mini-app.md)；未決與完成條件只在 [缺口](../architecture/governance/design-gaps.md) 更新。

## 目標架構驗收矩陣

| 範圍 | 必須證明 | 不能代替的證據 |
| --- | --- | --- |
| 分區 | 未登入、未註冊、pending、active、suspended、管理角色各正確；每個 URL 唯一 owner | 頁面存在、HTTP 200 |
| Layout | 全站／分區不提前讀不必要私有資料；單頁請求不拉全工作台 | 目錄符合示意 |
| 平行路由 | 站內導覽、直接深連結、刷新、返回／前進、未匹配 default、單區塊慢速／失敗 | 只有軟導覽成功 |
| 狀態生命週期 | 登出、換帳號、角色撤銷及延遲回應不顯示前身分資料 | 隱藏按鈕或父 layout gate |
| 模組責任 | 唯一用例 owner、單向依賴、client/server 隔離；合法與違規反例均有效 | AGENTS 存在 |
| 操作一致性 | 重送、並行、版本衝突、交易回滾、外部未知結果 | DB 管理者 CRUD |
| 效能 | 同版本環境與相同資料，對照可見／可互動時間、請求數、錯誤率，記錄樣本與網路 | build 時間、資料夾減少 |
| 真實整合 | 指定 LINE／Google／DB／部署及 Android／iOS 的允許、拒絕、取消與恢復 | SDK／API 替身 |
| 產品擴充 | [啟用條件](../product/mini-app.md#新功能啟用條件)定案，指定流程的來源、承接、確認、取消、重送及恢復可核對 | 選單、空頁或模型草稿 |
| 文件 | 主契約唯一、狀態不混用、引用有效 | docs:check 不驗 anchor 或業務 |

新增 slot 先固定觀測情境与比較基準，不事後挑最快樣本。慢速或錯誤注入要能證明其他可用區塊仍可操作，授權不足則不得洩漏資料。性能門檻未定案時先記量測，不宣稱加速。

## 證據格式

每筆結果記日期、來源 commit／工作樹識別、目標環境、輸入／前提、命令或操作、預期／實際、exit code、證據位置及限制。區分重新執行與快取、離線替身與遠端、UI harness 與真機；未知不寫通過。日誌不含秘密或私人正文。

文件整理不刪除恢復、資料切換或對帳所需紀錄。以下歷史內容不作新的操作指令；其中舊 API、目錄、圖片、方案與測試例外不能直接沿用。

## 2026-09-08 文件收斂驗收

範圍：21 份 docs、根 README、modules／shared README 及 14 份 AGENTS，共 38 份文件。主代理按文件責任、正反例與跨文件契約自行審閱；未讀產品程式，未宣稱獨立審查或產品驗收。保留工作樹中其他任務的配置、工具與版本更新，不歸為本次成果。

| 完成條件 | 結果 |
| --- | --- |
| 所有保留文件有獨立用途與索引入口 | 通過；docs 21 份均可由索引定位，產品擴充合併後原檔移除 |
| 主契約、README 與 AGENTS 分工一致 | 通過；移除局部重抄表格，舊目錄只定義過渡限制，不再要求 packages/ui |
| 歸屬與授權反例沒有互相矛盾 | 通過；中立 HTTP／業務投影、註冊恢復／active、群聊 context／團隊角色已分清 |
| 目標、未決項與歷史證據分開 | 通過；未決產品與落實工作留 design-gaps，歷史結果不作新操作指令 |
| 本地文件與引用檢查 | docs:check exit 0；另掃 38 份文件的 25 個本地標題引用，缺失 0、未索引 docs 0、已刪文件引用 0 |
| 差異格式 | git diff --check 在本次文件範圍 exit 0 |

六份 Web 局部 AGENTS 從 13,010 降至 6,683 字元（統一換行並排除尾端空白），為本次整理前後實測；不推論為令牌或執行效能改善。本次文件收斂驗收完成，產品決策待辦、程式遷移、靜態規則實作及線上／手機驗收不在此結論內。後續修改需重新核對受影響契約。

## 2026-09-07 文件收斂保留的歷史摘要

開發命令只由 [設定](../development/configuration.md#開發工具檢查) 維護；S1–S3 完成條件只由 [缺口](../architecture/governance/design-gaps.md#supabase-切換關卡未完成) 維護。功能案例依各整合契約選取，本文件記錄執行結果，不複製業務規則。

<details>
<summary>歷史證據：只在版本追溯、對帳或恢復時展開，不作開發預設輸入</summary>

以下由整理前主題文件轉入，未於本輪重新驗證：

- Supabase：2026-09-06 記載當時的 production environment 有 8 張業務表與 Google Provider；後來出勤向前 migration 20260906160213 的本地 13 表驗證不等於遠端套用。
- 清除前 SQLite 快照：1 會員、2 身分、3 會員事件、1 Coin ledger、0 打卡、1 支出、3 支出事件；不是目前資料筆數或再次清除授權。
- Redis：2026-09-06 測試 database 14600993／subscription 3406404，line-bot-cache、asia-east1；當次控制台記載 30 MB、30 connections、100 ops/sec、5 GB、US$0，無 HA／持久化，非 TLS 測試例外。原 10 次 PING 中位數 22.4 ms／最大 26.7 ms，去重後樣本 20.1／25.3 ms，不是 SLA 或現價。
- LINE 責任重整：2026-09-07 文件記載 validate 通過，Web 26、infrastructure 34、application 14、agents 4；181 modules／0 違規。1,038 組合成輸入離線前後比對相同，外部全為替身；未部署或真機驗收。
- AI：2026-09-06 文件記載收據單次辨識最多 1,200 output tokens、intake 最多兩輪、明確短問句一次；冷卻為程序內，不能作本輪跨實例配額證據。
- 舊檔案布局與素材是歷史實作說明。環狀八張選單為後續使用者決策，不由舊五張出勤素材紀錄推定現行發布。

## 歷史記錄

## 2026-09-06 LINE 無回覆事件（Redis 阻斷已排除，真人回覆待確認）

- Vercel CLI inspect 確認 Production alias 對應 `dpl_GS9TPzg1wdvf6FAPEN21YK9SiSGB`，建立時間台北 19:17:36。
- CLI production logs（最近 2 小時、限定 /api/webhooks/line）顯示 19:33 的兩次請求及後續請求均只有 events_received 且回傳 503。Production env ls 未列 REDIS_URL；目前 Webhook 先取得 Redis claim 才進行會員及回覆，因此缺少依賴即阻斷所有目標事件。
- Vercel Plugin project／logs 查詢被服務拒絕 403；以上證據來自本機已登入 CLI，不將 Plugin 查詢失敗當作沒有 log。
- 本地僅檢查 Redis 設定存在、URL protocol 及 test-only flag，未輸出秘密；目前是非 TLS 的測試 Redis，不能當作已有正式 TLS 接線。
- 本地新增 idempotency_unavailable 固定分類及回歸斷言；不記錄 URL、token 或聊天。不以繞過去重恢復回覆。Vercel 設定與重新部署尚未執行，線上恢復待驗證。

後續恢復：使用者明確同意沿用非 TLS 測試 Redis，已將 REDIS_URL、REDIS_KEY_PREFIX、REDIS_ALLOW_INSECURE_TEST 保存為該專案 Production Secret，CLI env ls 重新確認三個名稱存在。命名空間為 line-bot:vercel-test，未新增服務或啟用費用；此例外只支持測試用途，不是正式 TLS 驗收。

`scripts/pnpm.ps1 validate` exit 0 後部署 `dpl_91jEa4hAvmaTQtK6qWvYhKMoW8vD`，READY 且正式 alias 已切換。線上 health 200；合成未註冊身分的帶簽章圖片事件及相同事件重送均 200，runtime logs 確認首次 handled_silently、第二次重播完成結果。測試沒有發 LINE 訊息、下載圖片或呼叫 AI，短效去重 key 自動到期。這證明線上去重認領及完成可用；真人會員問答、手機登入及打卡仍待使用者驗證。


## 2026-09-06 非支出入口與 Web 目錄整理

- 範圍：乾淨工作樹起始，調整會員正式入口、獨立打卡 page／API、首頁 route group、components 與 shared；支出僅同步 import 與保留舊會員入口導向，未擴充業務。
- 本地 `scripts/pnpm.ps1 validate` exit 0。architecture 104 modules、0 violations；Web 8 個測試重新執行通過，涵蓋打卡路由、Origin、停權、重複獎勵及 LINE 入口。未變更套件的測試由 Turbo 快取重用，不聲稱重新連線測試資料庫。
- Next.js build 重新執行通過，輸出包含 `/attendance`、`/api/attendance`、`/membership` 與既有路由。`git diff --check` 通過。
- 限制：尚未實作群組管理，建立者／第一位管理者及加入授權待決。未建立或套用遠端 migration、未部署；新版跨路徑 LIFF 接續、Google 往返、真實手機定位未驗收。

## 2026-09-06 工作樹記載的接線結果

本節是工作樹中的結果記載，不單獨構成正式放行證據。完整採信前，仍須在每一項結果補上來源 commit／工作樹識別、環境／專案、命令或操作、預期／實際結果、exit code 與限制；下列「通過」只表示該記載的局部觀察，不表示 S1–S3 全數通過。

- 工作樹 `scripts/pnpm.ps1 validate` 型別、離線測試與正式建置通過，exit 0。
- Google Provider 已啟用；Google client 僅保留 Vercel origin 與 Supabase callback，已重新讀回確認。Supabase Site URL 與應用 callback allowlist 正確。
- Supabase 版本已部署至記載的正式網址；health 200、未登入會員 API 401、新 callback 200、舊 `/api/auth/line` 404。會員頁可導向 Google 帳戶選擇；未代替使用者完成個人登入。這證明部署與入口觀察，不證明產品正式放行。
- `scripts/supabase/verify-supabase.mjs` exit 0：受限 runtime、DDL／Auth email 拒絕、8 表 RLS、公開 API 隔離、TLS 與 Google 啟用狀態通過。
- 使用者要求空庫驗收，記載已清除匯入的舊業務紀錄，8 張表筆數均為 0。本地 SQLite 與備份刪除遭自動審查拒絕，仍在磁碟；產品不讀寫這些檔案。這是資料狀態記載，不等於已完成資料切換、備份清理或恢復驗收。
- 手機 Google／LINE 雙登入、session 更新與業務操作仍待使用者驗收；未標記 S1–S3 全數通過。

## 2026-09-06 LINE 主身分與選填 Google 部署

- 來源：本次未提交工作樹；Vercel deployment `dpl_26GBYrqUJL2TY7TooV7d3BGijdoY` 保存本次上傳版本，狀態 READY，正式 alias 為 `https://line-bot-963sup.vercel.app`。
- `scripts/pnpm.ps1 validate` exit 0：型別、離線測試與建置通過。測試涵蓋無 Google 的 LINE 註冊及日常 API、重複註冊、並行獎勵防重、owner 隔離、停權拒絕，以及選填 Google 的雙身分與衝突拒絕。
- `scripts/pnpm.ps1 exec vercel deploy --prod --yes` exit 0。部署後 HTTP 實查 health 200、未登入 membership 401，符合預期；桌面瀏覽器開啟 `/expenses?membership=1` 顯示 LINE 登入，沒有先進入 Google 登入。
- 本次未變更資料庫結構或清除業務資料。上述桌面入口觀察不等於手機 MINI 驗收；Android／iOS 的加入會員、重入不跳出、實際簽到與選填 Google 返回流程仍待驗證。前節 Google 強制入口屬本次修改前的歷史觀察。

## 2026-09-06 RLS grants 與索引遠端驗證

2026-09-06 09:21 UTC：Supabase Plugin 在當時的 production environment 套用 [migration 20260906092105](../../supabase/migrations/20260912023739_consolidated_baseline.sql)，回傳 success。CLI 先產生本地檔案；Plugin 套用時產生遠端版本，本地檔名依實查 history 對齊。重新查詢確認 anon／authenticated 對 `public.rls_auto_enable()` 的 EXECUTE 均為 false，`receipt_intents_owner_idx` 存在。security advisors 為空；performance 不再回報缺少外鍵索引，僅剩兩個 unused-index INFO。這次僅變更函式 grants 與新增索引，未操作業務資料，未驗證手機流程。

## 結果記錄

### 2026-09-06 開發邊界與首個 application 用例整合

- 來源：基於 `f5f384859f7173ad73b4cc22475539f921e7e390` 的未提交整合工作樹；驗證任務 `01a07627-3b6d-7a31-b2ed-7741ba411f06`，包含其他任務的 application 收件遷移與 tooling 修正，以及本任務的 Biome、architecture、LINE signature 循環拆解及格式整理。不是已發布版本。
- 環境：Windows、Node 24.19.0、pnpm 11.19.0；`VERCEL=1` 跳過本地環境檔，`TURBO_FORCE=true`。命令 `scripts/pnpm.ps1 validate` exit 0，typecheck 11/11、test 9/9、build 6/6 tasks 成功且均為 0 cached。
- 檢查結果：tooling 6 案例、Biome 118 檔、architecture 67 modules／0 violations；architecture fixture suite 驗證 source exports、type-only、ports、跨 app、client 間接引用 server／Route Handler／Node、dist import 及循環依賴。application 3、agents 4、infrastructure 11、Web 7 與環境載入 3 案例全部通過；Next build 成功。
- 檢查時發現 Windows wrapper 對隱含安裝未固定 store；其後修正所有命令的 store-dir，`scripts/pnpm.ps1 --version`、`store path` 與 `install --offline --frozen-lockfile` 均 exit 0，store path 為 D:\packages\pnpm-store\v11。此修正沒有改產品程式。
- 限制：CI YAML 已解析並核對唯讀權限及共用 validate 入口，尚未在 GitHub 執行；未 push、部署或驗證手機／遠端服務，未放行 S1–S3。其餘 application 用例仍依目錄設計逐项遷移。

### 2026-09-06 打卡／問答用例與 Knip 整合

- 來源：基於 `d693a8aa2dd0d26530a088443d51b1b5db7f2c83` 的未提交工作樹，起始工作樹乾淨；任務 `01a0763a-344b-7b32-ac71-1aeba693af50`。包含 application 打卡／問答 ports、Web composition、Knip、pnpm 安裝穩定性與文件同步。
- 環境：Windows、Codex bundled Node／pnpm，`VERCEL=1` 僅跳過本地秘密載入，沒有部署。`scripts/pnpm.ps1 validate` exit 0；tooling 6 案例、Biome 127 檔、architecture 73 modules／0 violations、Knip 無 findings；typecheck、application 7、agents 4、infrastructure 11、Web 8 與環境載入 3 案例、Next build 通過。
- 打卡沿用 membership API 與 PostgreSQL 交易測試，新增 application 拒絕無資格身分、缺地點、不信任前端 memberId／時間及原樣傳播交易失敗案例；問答新增分流、長度、冷卻、輸出上限與模型失敗案例，Web 測試確認任務別名／固定探測共用冷卻且零網路呼叫。
- 第一輪 typecheck 發現 LIFF global augmentation 需保留 module 標記，已修正。後續發現 Turbo 子 pnpm 自動安裝競爭，改為依賴不同步即失敗、wrapper 傳遞相同 store、固定專案 virtual store；明確 frozen install 後完整 validate 通過。最後僅調整 virtual store 設定，再次離線 frozen install、一般環境 deadcode 及 VERCEL 環境 validate 均 exit 0；該次 typecheck／test／build 重用同任務前一次通過的 Turbo cache。
- Knip 反例：暫時加入未引用的 application source，檢查回報該檔且 exit 1；移除後 deadcode exit 0。唯讀 code-mapper 與 change-reviewer 完成限定接線／差異核對，未發現可證實回歸；未替代主代理測試。
- 限制：未 push、執行遠端 CI、部署或手機驗收；S1–S3 及跨實例去重／AI 限流仍未放行。其他會員／支出用例、Agent SDK 完整 ports 化仍依目錄設計漸進處理。

### 2026-09-06 目錄分層與 Redis Webhook 去重

- 來源：基於 `d693a8aa2dd0d26530a088443d51b1b5db7f2c83` 的未提交工作樹；起始已含前述打卡／問答、Knip、會員修正及 AI 修改計畫工具。本次新增 domain 分目錄、infrastructure 按 persistence/auth/cache 分類、會員與支出 application 用例、Web auth/composition 接線，以及使用者追加要求的 Redis Webhook 去重；沒有把起始變更歸為本次實作。
- Windows bundled Node／pnpm：`scripts/pnpm.ps1 install --offline --ignore-scripts` exit 0，下載 0；workspace contracts 依賴及 lockfile 同步。完整 `scripts/pnpm.ps1 validate` exit 0：tooling、39 份文件／263 個連結、Biome 158 檔、architecture 98 modules／0 violations、Knip、typecheck、離線測試及 Next build 全通過；未變更套件重用同工作樹 Turbo cache。後續只維護驗收文件並重跑 docs:check。
- 純 application 測試覆蓋 active／owner／Google gate、日期來源、revision、辨識同步與失敗；Web 既有會員／支出／LINE 手機入口的 HTTP 合成案例保持通過。Webhook 離線測試以測試專用 fake store 驗證雙處理器共享 claim、pending 503、completed 重播、Redis 缺少／故障不執行副作用；production 無記憶體去重 fallback。
- `scripts/pnpm.ps1 exec node scripts/probes/check-redis.mjs` 對既有 `.env.local` Redis 測試接線使用唯一合成 namespace，exit 0。真實 Lua 跨兩連線 10 次認領恰有 1 winner、9 pending，completed 502／200 重播、TTL 到期重新認領及舊 owner completion 被拒均通過；去重 key 已 DEL，其他測試 key 短效到期。PING 10 次中位數 20.1 ms、最大 25.3 ms，只代表本次網路樣本。
- 限定唯讀 change-reviewer 未發現可證实的新回歸。整合中修正新辨識測試的同步時機、搬檔後文件斷鏈與兩個未使用的內部常數匯出，最終 validate 通過。Redis 原子 claim 不等於 exactly-once，失效界線見 [Redis](../integrations/redis.md)。
- 未推送、部署或修改 LINE 通道，未變更 DB schema／資料。實際 Vercel Redis 環境與 LINE 重送手機整合、S1–S3 仍未放行；聊天／OCR 的程序內冷卻未在本次 Webhook 去重範圍內改為 Redis。

### 2026-09-06 公開入口與個人路由結構遷移（本地）

- 驗證時基於 `d89164ded972ccaeef34f4b2e7051b2ae3c93e68` 的未提交工作樹；本批為公開根頁／工作台分離、/me/membership 與 /me/groups、/history 頁面所有權、LIFF 相容接續、共用 telemetry URL 清理與路由引用邊界。既有基礎設施目錄遷移及其他文件工作不歸為本批新增。
- Windows bundled Node／pnpm 執行 `scripts/pnpm.ps1 validate`，exit 0：architecture 161 modules／0 violations、架構反例、Knip、lint、typecheck、50 項套件／Web runtime 測試與 Next build 通過；未變更套件有 Turbo cache 命中。接續測試涵蓋 liff.state 等待、多重操作拒絕、無效 expense ID、固定目的地及不複製 token／code／外部 returnUrl。
- 本地正式模式 127.0.0.1:3127：11 個新舊入口 GET 200；公開頁可直接顯示介紹、未混入工作台，受保護頁回傳會員確認畫面；未帶 token 的 membership／attendance API 均回 401。只驗證 HTTP／SSR，不是瀏覽器 SDK 或手機接續驗收。臨時本地服務已停止。
- 未部署、修改遠端 LINE 設定或操作業務資料。WorkGroup、管理角色、真實 OAuth／LIFF 與 Android／iOS 驗收仍未完成。

## 2026-09-06 上班／下班拆分（本地）

本次工作樹將頁面與 POST API 分為 clock-in、clock-out；今日出勤保留唯讀查詢，LINE 指令、永久入口與六宮格素材同步拆分。既有 Profile 及根層文件修改不是本次新增成果。

- Windows 本地 `scripts/pnpm.ps1 validate` 通過：tooling、docs、lint、架構、Knip、型別、離線測試與 production build。部分未改動套件使用 Turbo 快取；受影響 Web、application 與 infrastructure 檢查有重新執行。
- API 測試驗證兩個 endpoint 拒絕 action 欄位、匿名、錯誤 Origin 與停權，並保留先上班再下班、定位範圍、重送不重複發 Coin、本人隔離。
- production server 綁定 127.0.0.1:3106 實測：舊 POST /api/attendance 回 405；兩個新 endpoint 的 GET 回 405；匿名 GET 出勤回 401；兩個新頁面回 200（呈現 gate，不代表授權成功）。Gateway 白名單同步驗證方法與路徑。
- 六宮格與登入接續、JPEG 格式檢查另跑 4 個測試全部通過。新版 JPEG 為 1527 × 1030、162862 bytes，已目視核對六格文字與位置。

尚未部署 Web、建立或啟用新版 LINE 選單；未執行真實手機定位、LINE 登入往返或遠端資料寫入。舊已發布選單的紀錄僅代表舊版本；本次新圖不可宣稱已在線上顯示。

## 2026-09-07 五態出勤與原子 API（本地）

本次以「出勤紀錄為權威、命令單一效果、圖片為可重試投影」實作早加、正常班、晚加、七支 POST API、五張六宮格、版本與 UUID 回執及持久排程入口。既有 Profile／根層使用者修改保留，不全部歸為本次新增。

- Windows bundled Node／pnpm 執行 scripts/pnpm.ps1 validate，最終 exit 0：tooling、文件、lint、架構與反例、Knip、型別、75 項套件／Web runtime 測試、Next production build 通過。測試為 agents 4、application 14、infrastructure 39、Web 15 及 env runtime 3；未修改套件有 Turbo cache 命中。
- 出勤測試涵蓋 08:00 早加原子交接、17:00 不自動加班、正常班原子接續晚加、跨午夜、必填原因／同意、同版本競爭、UUID 原樣重播及異樣拒絕、Coin 防重、停權與 Auth 停用、錯誤 Origin、匿名及多會員隔離。新排程入口另測未設定與錯誤 Bearer，拒絕時不開啟資料庫。
- 注入 audit 寫入失敗，驗證區段、命令、事件、outbox 與 Coin 全部回滾。租約測試涵蓋過期結果重新排入最新選單、退避重試；21 位到期會員測試驗證前 20 位失败不永久擋住後續會員。
- PGlite 依排序套用所有 migration，驗證 13 張表啟用 RLS、瀏覽器角色拒絕。向前遷移測試保留舊時間、舊表與 ledger，不製造新定位事件；孤立下班、多個開放區段拒絕遷移。PGlite 的序列化測試不是遠端 PostgreSQL 多連線或雲端角色驗收。
- 五張 JPEG 已目視核對六格文字，大小 ready 119586、early 118503、working 121195、finished 121842、late 121923 bytes；本地 preview 已有五份配置與圖片雜湊。未執行本次 create／activate。
- 隔離 LIFF／API harness 的五個狀態頁回 HTTP 200；當前 CUA 沒有可用瀏覽器 surface，未完成 390px 截圖、表單點擊或真實 LIFF／手機定位驗收。這些 fixture 與 HTTP 結果不能當作瀏覽器功能通過。臨時服務已停止，本地證據位於忽略的 .artifacts/attendance-ui-browser-evidence.md。

尚未套用遠端 20260906160213 migration、部署 Web／排程、設定五個 menu ID 或發布五張選單。需另經授權後執行並核對備份、舊資料、排程容量、LINE 同步與實機效果。請假、補登、待辦、即時回饋及群組服務未由這批出勤規則自動擴充。

## 2026-09-07 Rich Menu 素材清理與瀏覽器補驗

- 經使用者明確要求清理冗餘，移除 assets 中舊版 PNG 與 JPEG，只保留五張 work-assistant 狀態圖，历史由 Git 保存。移除測試對舊檔的引用，改為核對現用檔案集合與逐張 JPEG 驗證；3 項 rich-menu 測試、docs:check、diff --check 通過。
- 本次補正出勤成功回應驗證、初始化過期讀取防護、操作中同步阻擋與手機加班表單樣式；修改後完整 validate exit 0。使用環境內已有 Playwright 與 Edge，在 390 × 844 視窗實際操作本地 production server 加隔離 LIFF/API 替身，五態可呈現、原因與早加同意必填、17:00 提示、200 不完整回應保留原 UUID/payload 重試皆通過；未捕獲 pageerror，無水平溢出。截圖位於忽略的 .artifacts/attendance-<state>-390.png，驗證腳本 .artifacts/verify-attendance-browser.mjs。臨時伺服器已停止；這不是真實 LINE 手機定位或遠端業務驗收。
- 經唯讀 LINE API 重新查證：遠端只有舊版「工作助手六宮格」，預設仍是 richmenu-7e4701d286e73faa2d95863433292c5a。本地五個 LINE_RICH_MENU_*_ID、worker secret 及工作地點未設定。七張本地圖片不等於七張遠端選單；新版五張未上傳／啟用是實測聊天仍顯示舊選單的直接原因。未執行遠端變更，也未以本地圖片清理宣稱線上已修复。

## 2026-09-07 入口與套件精簡（本地）

本次工作樹依使用者確認保留功能與 URL，將七個舊頁面統一為 system 接續入口，會員／Google 與群組由 me 擁有；移除三個只有規範的套件目錄及單行 URL adapter，SQL fixture 改用明確測試匯出。

- Windows 本地 `node scripts/validate.mjs` exit 0：tooling、文件、lint、架構及反例、change-plan、Knip、型別、離線測試與 Next production build 全部通過。架構分析 182 個模組、0 違規；未修改套件使用 Turbo 快取。Next 舊 dev 型別快取引用搬移前路徑，移至忽略的 artifacts 後，以目前路由重新生成型別通過，未放寬 tsconfig。
- 使用既有 Playwright／Edge headless 操作 127.0.0.1:3197 的 production server，23 個情境通過：七個別名、四個 Google 參數接續、expenses 註冊／恢復與模擬 LIFF 參數還原、未註冊／pending／suspended／服務失敗／未登入、正式頁保留無關 LIFF 參數、衝突意圖、等待 SDK 及 feedback 入口。確認會員查詢發生於目的頁，無頁面例外、無 API 寫入請求。
- 瀏覽器使用合成 LINE SDK 與會員 API；其他遠端請求阻擋。證據為忽略的 .artifacts/structure-simplification-validation.log、structure-browser-results.json 與 verify-structure-browser.cjs。這些結果不代表真實 LINE SDK、Google OAuth、手機或遠端 PostgreSQL 驗收；未部署或修改遠端設定。

## 2026-09-07 會員與 HTTP 責任清晰化（本地）

本次保留既有 URL、API 與資料狀態，拆分個人檔案、註冊／恢復與 Google 關聯呈現，共用原有 membership API 流程；HTTP 工具集中到 server/http，會員錯誤紀錄分類留原責任。pending／active 的註解及資格提示改為既有 LINE 主身分語義；Google 維持選填。未實作的 Workspace 結果頁設計統一為 auth/google-workspace/result。

- Windows 本地完整 validate 通過：tooling、docs、lint、架構與反例、change-plan、Knip、型別、離線測試與 production build。首次 build 因另一個程序持有 Next 建置鎖失敗，待鎖釋放後重跑通過，未刪鎖或終止其他程序。部分未修改套件使用 Turbo 快取。
- 架構反例驗證相容路徑缺少（含整個 app 缺少）、錯誤目的地、額外資料呼叫與子檔案均拒絕；區域名稱、引號及屬性順序變更可通過。HTTP 回歸測試核對既有 status／code／retryable、隱私標頭、未知錯誤遮蔽、固定 Origin、JSON object 與 2048 bytes 邊界。
- 本地 production server 加既有 Playwright／Edge，在 390 × 844 視窗通過 10 個會員操作情境：註冊、恢復、active 不重註冊、停權拒絕、Coin 簽到、暫停取消及原頁恢復、Google 外開、Google 登入提示與取消、Google 確認失敗重試、註冊失敗重試。核對請求 path／body、成功留原頁、Google 確認帶合成雙證明及清除 query；無 pageerror 或水平溢出。
- 同時重跑 23 個相容入口／LIFF／會員狀態瀏覽器情境，全部通過。所有 API 與 LINE SDK 使用合成替身，其他遠端請求阻擋；這些操作沒有寫入真實資料或發 LINE 訊息。臨時服務已停止。
- 本地證據位於忽略的 .artifacts/responsibility-validation.log、membership-browser-results.json、verify-membership-browser.cjs 與 structure-browser-results.json。尚未部署，真實 LINE／Google OAuth、Android／iOS 裝置驗收仍待執行。

</details>

## 2026-09-09 團隊協作首版

來源為基底 `f028b5d` 的本地工作樹（含同 checkout 的公告／任務模組），不是已部署版本。設計見[團隊協作](../product/team-collaboration.md)。新增 `/team`、`/api/team`、首頁入口及三個 LINE 白名單接續；公告共用 announcements，交接共用 tasks。HTML 雛型仍只作產品設計附件。

- `scripts/pnpm.ps1 validate` 完整通過：工具、文件、lint、架構反例、變更計畫反例、Knip、型別、測試與 build。架構 226 modules／0 violations；Turbo 對未變更套件使用快取，本次 Web 正式 build 實際執行。執行紀錄留於本地 `.artifacts/team-validation.log`。
- [隔離資料庫測試](../../packages/infrastructure/test/team.test.ts)以 PGlite 執行全部 migration 並以 line_app 存取：加入前拒絕、跨團隊、角色／最後管理者、撤回與歷史、同來源公告可見性、退回不轉移、同 UUID 並行重送只轉移一次、task_events／version、非參與者拒絕、更正重新確認、停權／移除及 runtime 不可更新命令紀錄。注入回執保存失敗後，團隊與成員寫入一起回滾。
- [用例測試](../../packages/application/test/team.test.ts)確認無效輸入在建立資料庫 adapter 前拒絕；[入口／HTTP 測試](../../apps/web/test/team-entry.test.ts)確認三個固定意圖、重複意圖拒絕、登入去除秘密參數、Origin／格式／大小限制與錯誤脫敏。
- Next.js 正式模式 `127.0.0.1:4105`，Edge headless 390px／1280px：公告發布與 503 原命令重試、會議新增／確認、更正取消零 POST、換帳號清除正文、無水平溢出與無 pageerror 通過。瀏覽器的 LINE SDK／API 使用合成替身；畫面實際來自 `/team`，不把此證據當真實 LINE 登入或 DB 串接。腳本與截圖留於本地 `.artifacts/team-browser-check.cjs`、`team-mobile.png`、`team-desktop.png`。

未驗證：遠端 migration／權限、Vercel 發布、LINE 實體選單與 Android／iOS。PGlite 不證明遠端多連線競爭；正式 PG 鎖定、角色與恢復仍依 S2／S3 驗收。尚未決定的保存／刪除期限仍為 PG2；沒有啟用錄音、AI、推播、外部同步或 LINE 群組綁定。既有素材的規劃中標記未更換。

### 2026-09-09 最小方案複核

來源為基底 `72c6a1f` 的本地工作樹。保留公告／任務單一來源、明確交接與會議版本確認，不新增框架、資料表或狀態管理層。TeamPanel 沿用既有 generation 檢查，補足登入核驗等待後、讀取資格失敗與送出後清單失敗的遲到回應防護。

- 完整 `scripts/pnpm.ps1 validate` exit 0；Web 重新 build，六個 build 任務中五個使用 Turbo 快取。紀錄為本地 `.artifacts/team-refinement-validation.log`。
- Edge headless 重跑上述互動檢查通過，並加入「保存成功後延遲清單回應，切離頁面，再回傳 503」：舊成功提示已清除，遲到失敗不恢復錯誤提示。初次斷言將 Next.js 空白 alert 節點算作錯誤，改為檢查實際提示文字後通過。
- 測試仍使用合成 LINE SDK／API，未執行遠端 migration、部署或真機驗收。

### 2026-09-09 正式 migration 與 Web 發布

發布來源為 `72c6a1fb2beaa5b52f659597bdef2100b934289f`，已推送 GitHub main。使用者明確授權後，以 Supabase CLI db push 對當時的 production environment 套用四筆 migration：20260908164610、20260908164718、20260908165129、20260908170000。遠端 history 回讀一致，再次 dry-run 顯示 upToDate、無待套用項目；未執行 seed、roles 匯入或 Vault 更新。

- 遠端 catalog 確認 12 張新表存在且啟用 RLS，anon／authenticated 無直接讀寫權限；line_app 的讀寫限制符合 migration。使用本地配置的受限 runtime 連線，切換 line_app 後對 12 張表執行 LIMIT 0 成功，未讀寫業務列。此查核不代替真實會員跨團隊業務驗收。
- GitHub [Validate](https://github.com/963sup/line-work-assistant/actions/runs/34260649485) completed／success。
- Vercel production `dpl_F2jL3KxXysvFLewpU4Q6cfFgpUh9` Ready，正式 alias 為 https://line-bot-963sup.vercel.app 。health 與 home／notifications／tasks／team 回傳 200；三個新業務 API 未登入均回傳 401。頁面 HTTP 成功不代表完成 LIFF 登入或操作。
- 未驗證：Android／iOS、有效會員端到端寫入、遠端多連線競爭與恢复；未發布 LINE 圖文選單或執行公告推播。發布操作腳本留於忽略的 .artifacts/release-migrations.mjs 與 release-schema-check.mjs。

## 2026-09-09 視圖 URL 同步（本地）

本次沿用既有頁面路徑、API 與資料模型，將公告 `category`、任務 `taskView`、團隊 `teamView` 與畫面雙向同步，並保留團隊 `handovers=1`／`meetings=1` 舊入口。登入接續只保存單一有效視圖值；視圖參數不授予發布或管理權限。契約見[入口與存取](../architecture/web/entry-and-access.md)。同步更新選單、任務與團隊文件，區分外部表單入口、已實作功能與待驗收範圍。

- `scripts/pnpm.ps1 validate` 完整通過：工具、文件、lint、架構與反例、變更計畫反例、Knip、型別、測試與 production build。架構 226 modules／0 violations；Web 40 個測試通過。未變更套件使用 Turbo 快取，Web 正式 build 實際執行。
- 本地 Next.js production server `127.0.0.1:4117` 加 Edge headless（390 × 844）通過三個模組的直接進入、分頁點擊、返回／前進、重新整理、同路徑 query 變更與無效參數回退；另驗證任務同分頁返回清單且不重複增加 history、發布取消後重新整理不重開、團隊舊入口優先於衝突的新視圖參數。
- 三個模組均驗證寫入等待期間切換網址、回傳 503、明確重試仍使用原命令，以及完成後套用目前網址。任務發布途中切到「我的任務」不被舊發布流程覆寫；發布資格不足不開啟表單，公告 API 拒絕管理資格時不顯示新增操作。瀏覽器無 pageerror。
- 瀏覽器使用合成 LIFF SDK／API 回應及合成寫入，其他遠端請求阻擋；本地腳本為忽略的 `.artifacts/url-navigation.cjs`。此證據不代表真實 LINE SDK、正式後端串接或手機驗收。本次未部署、未執行 migration、未更換 LINE 選單，也未提交外部表單。

### 2026-09-09 URL 導覽補強與可重跑測試（本地）

來源為基底 `d473300` 的本地工作樹，接續上節視圖 URL 同步。公告、團隊重複點擊目前視圖不再新增 history；同視圖的非法／重複參數或舊團隊意圖正規化使用 replace，切換視圖仍使用 push。測試移為版本控制內的 [瀏覽器腳本](../../scripts/browser/check-navigation.mjs)，執行方式見[操作文件](../../scripts/browser/README.md)，不再綁定個人機器的 Playwright 路徑。

- 完整 `scripts/pnpm.ps1 validate` 通過，Web 40 個測試、架構 226 modules／0 violations，Web production build 實際執行；其他未變更套件使用 Turbo 快取。首次在沙箱內因 Node 測試子程序 `spawn EPERM` 失敗，於允許啟動子程序的環境重跑通過，未跳過檢查。紀錄位於本地忽略的 `.artifacts/url-navigation-followup-validation.log`。
- 新腳本使用 Playwright 1.62.1、Edge headless 390 × 844、本地 `127.0.0.1:4117` 通過：保留上節三模組案例，新增公告／團隊同視圖點擊不增 history、非法／重複視圖參數正規化，以及舊 `handovers=1` 轉為同視圖 `teamView` 不增 history。無 pageerror。
- 新腳本加入後補跑 lint（317 files）、Knip 與文件檢查通過；瀏覽器測試為獨立手動入口，未宣稱 CI 自動執行。LIFF／API 仍為合成替身，沒有真實業務寫入、部署、migration、LINE 選單發布或手機驗收。

## 2026-09-09 任務與公告詳情深連結（本地）

來源為基底 `d473300` 的本地工作樹。新增 `/tasks/[taskId]` 與 `/notifications/[announcementId]`，列表點入、登入接續、重新整理及返回可保留目標資源與合法列表視圖。公告詳情以一般頁面區塊共用既有內容／操作，列表新增公告保留對話框。路由與失敗契約見[入口與存取](../architecture/web/entry-and-access.md#url-與功能-owner)。

- 公告沿用 `/api/announcements` 的 ID 授權查詢；任務新增唯讀 `GET /api/tasks/[taskId]`，沿用 TaskSnapshot 投影但只回該團隊、目標任務及其歷程。原有列表與 POST 命令不變，沒有 migration。任務 ID 由用例驗證 UUID 並正規化，查詢先核驗身分、查所屬團隊，再取得群組鎖與核驗成員資格，不在群組鎖前鎖任務列。
- 完整 `scripts/pnpm.ps1 validate` exit 0：lint 320 files、架構 229 modules／0 violations、Web 42 tests、application 18 tests、infrastructure 49 tests，以及其餘工具／反例／Knip／型別與 production build 通過。未變更套件使用 Turbo 快取；Web build 實際執行。紀錄為忽略的 `.artifacts/resource-detail-validation.log`。
- 新增用例與 PGlite 測試驗證 UUID、失效會員、跨團隊、移除成員、停權、身分不符、不存在與單一任務／事件回傳。這些是隔離資料庫測試，不代表正式 PostgreSQL 的多連線競爭已驗收。
- [瀏覽器腳本](../../scripts/browser/check-navigation.mjs)於本地 Next production `127.0.0.1:4117`、Playwright 1.62.1／Edge headless 390 × 844 通過既有視圖案例與兩種詳情的列表點入、直接開啟、刷新、返回、錯誤／資格拒絕、原命令 503 重試及最新 URL 保留；無 pageerror。首次返回測試在連結尚未切换網址時便按返回，改為先斷言列表 URL 生效後通過。審查找到公告寫入完成使用舊分類的問題，已改由完成當下 URL 決定返回分類，並有在途 query 變更與重試案例。
- 未使用 mock 的本地任務詳情 API 未登入探測回傳 401 與 `Cache-Control: no-store`；未使用真實 LINE 憑證或呼叫業務資料庫。瀏覽器 SDK／業務 API 仍為合成替身，所有寫入均為測試替身，不代表真實登入或後端端到端串接。

本次未部署、未提交外部表單、未發布 LINE 選單，未新增 API 版本層、平行路由、異常案件或表單審核系統。Android／iOS、正式會員跨團隊與遠端競爭驗收仍待完成。

### 2026-09-10 合作夥伴管理資格與唯讀查詢（本地）

來源為 `8db3b9d` 上的本次工作樹。`/admin/groups` 接入既有 LINE 身分驗證、
會員與獨立管理名單；每頁 20 個夥伴，可展開刊登／下架窗口。產品流程見
[合作夥伴管理查詢](../product/team-collaboration.md#合作夥伴管理查詢)。

- 完整 `scripts/pnpm.ps1 validate` 通過：架構 254 modules／0 violations、Web 50 tests、
  infrastructure 60 tests、型別與正式 build；未變更套件部分使用 Turbo 快取。
- PGlite 以受限 line_app 驗證獨立管理資格、停權、撤權後分頁拒絕、同名分頁、
  下架及無窗口資料、一般會員仍只讀公開名錄；runtime 不能自授權，瀏覽器角色無權讀名單。
  此結果不代表遠端 PostgreSQL 多連線競爭已驗證。
- HTTP 測試使用合成 LINE 證明，核對缺 token 拒絕、前端 memberId／role 不影響 actor、
  資格不足回 403、來源失敗回 503 且不洩漏內部錯誤，回應 no-store。
- `scripts/pnpm.ps1 test:browser` 通過；本地 Next production、Edge headless，新增管理案例
  驗證明細展開、分頁、資格拒絕、服務失敗重試、讀取中換 token、刷新與返回。
  手機 390 × 844、桌面 1280 × 900 截圖及 trace 保存在
  `.artifacts/browser/2026-09-10T14-49-28-316Z`。LINE SDK／API 為合成替身，外部請求阻擋。
- 本地 migration `20260910144348_partner_managers.sql` 未在遠端套用，未授予任何真實會員
  管理權、未部署、未發布 LINE 資源，Android／iOS 真實 LINE 登入及管理查詢仍待驗收。

### 2026-09-10 合作夥伴多窗口維護（本地）

來源為 `8db3b9d` 上的工作樹，接續上述唯讀階段。`/admin/groups` 可新增、編輯、
保存多窗口及 phone／email／line，並上下架夥伴或窗口；產品及資料規則依
[合作夥伴管理契約](../product/team-collaboration.md#合作夥伴管理查詢)。

- 完整 `scripts/pnpm.ps1 validate` exit 0：架構 255 modules／0 violations、Web 50 tests、
  infrastructure 64 tests、型別及 production build 通過；部分未變更工作使用 Turbo 快取。
- PGlite 驗證独立管理權、撤權後重試拒絕、原命令回執、版本衝突、保存與稽核共同回滾、
  既有窗口不可刪除／移轉、同公司不同窗口共用電話，以及同次下架舊窗口並建立替代窗口。
  migration 測試確認舊聯繫值、夥伴與窗口 ID 保留；未驗證遠端 PostgreSQL 多連線競爭。
- HTTP 使用合成 LINE 證明，驗證管理 POST 的來源、token、偽造身分欄位、同意與 actor。
- `scripts/pnpm.ps1 test:browser` exit 0：本地 Next production／Edge headless 通過新增三管道、
  第二窗口、下架、409、送出前換會員拒絕、遺失回應後刷新不自動送出、原命令重試一次保存。
  截圖及 trace 位於 `.artifacts/browser/2026-09-10T15-08-43-723Z`；LINE／API 為合成替身。
  首次瀏覽器測試因狀態選單定位過於嚴格失敗，修正定位後通過；新增窗口替代測試的預期數量
  修正後，完整驗證重跑通過。
- 新 migration `20260910145329_partner_directory_management.sql` 只在本地測試套用。
  未遠端遷移、未授予真實管理者、未部署或發布 LINE 資源；真實 LINE 手機驗收待完成。
  舊版與新 schema 不相容，發布順序依[放行與復原](../operations/release-and-recovery.md#合作夥伴管理查詢放行)。

### 2026-09-10 會員管理與可追溯停權（本地）

來源為 `8db3b9d` 上的工作樹。`/admin/members` 與 `/api/membership/manage` 接入獨立名單、
會員 ID 查詢、狀態分頁、Google 關聯摘要、影響清單與停權／解除；業務依
[會員管理契約](../integrations/membership.md#管理查詢與停權)。

- 完整 `scripts/pnpm.ps1 validate` exit 0：架構 262 modules／0 violations、Web 51 tests、
  infrastructure 67 tests、型別及 production build；未變更工作部分使用 Turbo 快取。
- PGlite 驗證查詢／停權權分離、自停權拒絕、前態恢復、舊 suspended 回 pending、原命令回執、
  版本衝突、撤權後重试拒絕、瀏覽器／runtime 不能改授權或刪回執、Coin 不重置。
  本人暫停／恢復推進版本；停權拒絕註冊、恢復及簽到；稽核失敗回滾，未結束出勤不自動關閉。
- HTTP 使用合成 LINE 證明驗證可信 actor、來源、缺 token、偽造 actor／重複 query、403／503
  與 no-store，未呼叫真實 LINE 或業務資料庫。
- `scripts/pnpm.ps1 test:browser` exit 0；本地 Next production／Edge headless 390 × 844 與
  1280 × 900，LINE／API 為合成替身，外部請求阻擋。驗證 ID／狀態／分頁、影響明細、停權／解除、
  斷線後刷新不自動送出、原命令重試、409、換會員拒絕、唯讀／撤權、失敗重讀與返回。
  最後補上篩選欄位保留目前條件，經格式檢查、重新建置與瀏覽器回歸通過。
  畫面、日誌及 trace 位於 `.artifacts/browser/2026-09-10T15-47-39-735Z`。
- 初次檢查發現 port 放錯層及測試型別問題，移至既有 ports 邊界並修正型別；出勤 fixture
  日期不符既有 guard，改正合成日期後重跑通過，未放寬架構或出勤限制。
- `20260910153250_member_management.sql` 只在本地 PGlite 套用，未遠端遷移、配置真實管理者、
  部署或發布 LINE 資源。正式 PostgreSQL 多連線的停權／恢復／簽到競爭、真實手機仍待驗收。
  其餘出勤、支出、稽核、設定四個管理頁仍為外框，未宣稱整個後台已完成。

### 2026-09-11 返回主選單遠端修復

來源版本為 `8db3b9d`，正式站為 `line-bot-963sup.vercel.app`。返回按鈕的
`attendance-menu` postback 已正確發布；Webhook 驗證回 200，但實際事件日誌出現
`events_received` 後回 502。遠端缺少工作地點資料表，使出勤 snapshot 在選單同步前失敗。

- 官方 Supabase CLI 隔離預演後，僅套用既有 `20260910141155_workplaces.sql`；
  沒有套用其他待發布 migration、匯入資料或授予真實管理者。
- 以正式 runtime 的 `line_app` 角色讀回工作地點表成功，當時為 0 筆。
- 確認指定測試身分為 active 會員且無待送通知後，送出合成簽章返回事件；
  正式 Webhook 回 200，LINE 個人選單 GET 回 200 並讀到既有上班主選單。
  此為後端與 LINE API 驗證，Android／iOS 實際點擊仍待確認。
- 發布前目前工作樹完整 `scripts/pnpm.ps1 validate` 通過；Web 51 tests、
  型別及 production build 通過，架構 262 modules／0 violations，部分套件使用快取。
  工作樹另有會員與合作夥伴管理變更；該完整檢查不代表這些功能已發布。
- 本次修復沒有程式差異；Git 僅新增本節證據。

### 2026-09-11 會員與合作夥伴管理修正及遠端遷移

來源為 `c21f77f` 上的 42 檔工作樹，Supabase 目標為當時的 production environment。

- 修正 UUID 大小寫繞過窗口重複檢查、既有非標準聯繫值無法原樣保存或下架、
  查無會員後無法清除查詢，以及管理回應多送 `canReview`。
  舊值保留由後端比對資料庫原值；新增或變更值仍須通過格式驗證。
- 完整 `scripts/pnpm.ps1 validate` exit 0：架構 262 modules／0 violations、
  infrastructure 68 tests、Web 51 tests、型別與 production build 通過，部分工作使用 Turbo 快取。
- `scripts/pnpm.ps1 test:browser` exit 0：本地 Next production／Edge headless，
  390 × 844 與 1280 × 900；涵蓋導覽、會員、出勤、工作地點、合作夥伴與會員管理，
  包含查無會員後清除條件恢復查詢。LINE／API 為合成替身，外部請求阻擋。
  截圖與 trace 位於 `.artifacts/browser/2026-09-11T10-56-36-566Z`。
- 遠端遷移前確認夥伴相關資料表尚不存在，保存既有會員狀態快照；官方 CLI 預演後套用
  `20260909170000_partners.sql`、`20260910144348_partner_managers.sql`、
  `20260910145329_partner_directory_management.sql`、`20260910153250_member_management.sql`。
  再次預演無待套用 migration；欄位、`line_app` 讀取與禁止自行授權、匿名不可讀管理表均驗證通過，
  原有會員狀態不變。快照僅供本次核對，未演練完整資料庫還原。
- 兩種管理名單均為 0，未授予真實帳號管理權。正式 PostgreSQL 多連線競爭、
  真實 LINE 手機流程仍待驗收；此紀錄不代表 Git 推送、Web 部署或 LINE 資源發布。


## 2026-09-12 功能權限第一版本地驗收

- 新增 `/admin/permissions`、`/me/permissions` 與 `/api/permissions`；管理首頁依資格呈現入口。
  已實作功能改用固定 permission_grants；指定地點限制列表、明細及寫入，打卡名單與團隊角色保留原來源。
- 本地 migration `20260911175550_permissions.sql` 將五種舊管理名單轉為六項業務能力，保留原範圍及授權來源，
  移除舊權威表；未自動任命權限管理員。任命由可信操作者負責，Web 不可自授權或修改自己的業務權限。
- PGlite 覆蓋舊名單遷移、跨地點拒絕、版本衝突、原命令重播、授權稽核失敗回滾、
  停權恢復後舊權失效、最後管理員暫停／停權／刪除保護、瀏覽器角色及 runtime 任命限制。
  會員狀態與權限異動使用相同鎖順序；本地 PGlite 不能代替正式 PostgreSQL 多連線競爭驗收。
- 完整 `scripts/pnpm.ps1 validate` 通過，日誌 `.artifacts/permissions-validation.log`；
  另有針對新增稽核及會員狀態保護的 `.artifacts/permissions-targeted.log`。
- `scripts/pnpm.ps1 test:browser` 通過；Next production 與既有 Chrome，LINE／API 合成並阻擋外部請求。
  驗證授權確認、指定範圍、結果未知後重新整理並重送同一命令、撤銷、禁止自改、換帳號清理及管理導覽。
  同時通過原導覽、會員、出勤、地點、夥伴及會員管理回歸。
  產物 `.artifacts/browser/2026-09-11T18-22-18-200Z` 包含 `permissions.png`、`permissions-trace.zip` 與 JSON 結果。
- 未執行遠端 migration、管理員任命、Git 推送、Web 部署或 LINE 發布；正式手機仍待驗收。
  權限切換需依發布恢復文件協調新版 Web，不能只套用 migration 後繼續使用舊版。

### 2026-09-12 正式環境切換驗收

- 經使用者明確同意短暫停機，02:39:04–02:39:18（Asia/Taipei）完成 migration、首次管理員任命與部署切換，並恢復 runtime 角色。
- Supabase migration history 已確認 `20260911175550_permissions`；指定帳號的權限管理員資格及全域 `workplaces.manage` 均有效，授權有來源與稽核紀錄。
- Vercel 正式網域已解析至 Ready 部署 `dpl_496kbePPBGmRRaKcMS3TjaEWrikZ`，由本地已驗證程式部署，未執行 Git 推送。
- 使用既有 runtime 連線唯讀驗證權限及工作地點 repository 成功；正式 `/admin/permissions`、`/me/permissions`、`/admin/workplaces` 回應 200，無 token 的 `/api/permissions` 回應 401。
- 切換與查核產物：`.artifacts/permissions-release.json`、`.artifacts/permissions-verify.json`。尚未以真實 LINE 登入及手機操作驗收，亦未發布 LINE 平台資源。


## 2026-09-12 SAP 第一段雛形

- `/sap` 提供專案入口、WBS／預算、合約及待核准畫面；估驗與收付款僅有明確標示尚未開放的路由。
  模型及責任邊界見 [SAP 設計 2.14](../architecture/sap/sap-engineering-system-design.md#214-第一段雛形實作狀態)。
- 現有 domain／application／contracts／infrastructure 套件各自建立 sap 業務子模組；Web shared 僅放中立 JSON request 檢查。
- `20260911184006_sap_foundation.sql` 建立私有資料表、角色／專案鎖及最小 runtime 權限；
  `scripts/sap/seed-demo.sql` 必須由操作者指定三位既有有效會員，未任命真實角色。
- SAP 模型、API、PGlite 測試通過，涵蓋定點金額、拒絕偽造欄位、自我核准、跨專案、版本、重播、
  預算占用超額、被引用 WBS 凍結、稽核失敗回滾與資料表權限。日誌 `.artifacts/sap-tests.log`。
- 本地 PGlite 不能代替正式 PostgreSQL 多連線競爭；未套用遠端 migration、部署、接通 SAP 或真實 LINE 手機驗收。

## 2026-09-12 LINE 私聊建立工作地點

- 操作契約見 [LINE 私聊新增打卡地點](../integrations/line.md#私聊新增打卡地點)。
  新增指令、原生位置訊息、半徑與版本化確認按鈕已接至正式／相容 webhook 共用處理。
- 本地 `scripts/pnpm.ps1 check` 通過：lint、308 modules／0 架構違規、型別；
  application 25、infrastructure 82、Web 58 測試通過，另有環境載入 3 與 agents 4 測試。
- 新測試涵蓋無權限拒絕、私聊隔離、同批事件順序、逾期、舊按鈕、無效位置／半徑、
  確認前不建地點、事件重播、建立失敗整筆回滾及 RLS。既有權限 migration 測試固定到其目標版本。
- 本地正式 `build` 通過；架構／變更規劃測試 12 項及 deadcode 通過。
- 文件檢查通過：293 Markdown、546 本地連結；先前缺失的 SAP 驗證文件已補齊。
- 最後完整 `scripts/pnpm.ps1 validate` 通過，包含工具、文件、靜態、測試與正式建置。
- `20260911190349_workplace_chat.sql` 僅完成本地 PGlite 驗證；未套用遠端 migration、
  未部署或真實發訊，也未驗收 Android／iOS 原生選點及 MINI App 接續。
- `scripts/pnpm.ps1 check` 通過：lint、架構（308 modules／0 violations）、型別及完整測試；
  日誌 `.artifacts/sap-check.log`。文件檢查通過；v0.8 文件／算術驗證 519 項通過，與執行期驗證分開計算。
- SAP 專項瀏覽器通過：真實 Next production build、Edge，合成 LIFF／API；WBS 建立、預算原命令重試與核准、
  合約表單、深連結、同 token 換 Member 拒絕及無權限清理。產物 `.artifacts/browser/2026-09-11T19-14-46-948Z`
  含手機／桌面截圖、`sap-results.json`、trace 及 report；隔離 runner 保存於 `.artifacts/sap-browser-only.mjs`。
- 最新完整 `test:browser` 停在既有 auto-clock 的 LINE 初始化／alert 等待（`.artifacts/browser/2026-09-11T19-12-27-281Z`），
  因此本次不宣告完整瀏覽器回歸通過；SAP 專項通過不替代此項。
- 最終完整 `scripts/pnpm.ps1 validate` 通過（含工具、文件、架構反例、deadcode、型別、測試與建置），
  日誌 `.artifacts/sap-validate.log`；工作樹含其他進行中功能，本次未提交或發布。

### 2026-09-12 SAP 專案列表補齊

- 新增 `/sap/projects`，列出目前會員可存取專案的代碼、名稱與角色；工程內頁提供返回列表入口。
- lint、架構、型別、完整測試及文件檢查通過；測試子程序因沙箱 EPERM 改在核准的本機執行環境重跑。
- 完整 `test:browser` 通過，含既有出勤案例及 SAP 專案列表導覽；production build、合成 LINE／API。
  產物 `.artifacts/browser/2026-09-11T19-47-12-587Z`，列表截圖 `sap-project-list.png`。
  本次結果補足上次完整瀏覽器回歸未通過的證據；不代表遠端部署或真實 LINE 驗收。

## 2026-09-12 按業務拆分目前 SQL 結構（本地）

- 使用者要求改善開發與閱讀，採用 [Supabase 開發入口](../../supabase/README.md)：11 份 schemas 直接描述最終結構，各自包含表、約束、索引、grants、RLS 及函式／trigger；不重演舊表轉換。
- 重新讀回指定專案的 migration history，僅有 `20260912023739 / consolidated_baseline`。移除工作區恢復出的重複舊檔與 `20260912023459` 副本，保留真正基線；其 SHA256 前後均為 `18a79fe60c51653b127e27ca36b70a8a6166ab559ce5a71411a82d8cde07665e`。本輪沒有遠端寫入或資料清除。
- `schema:check` 通過 2 項本機工作流程檢查與 1 項完整 PGlite 結構等價檢查。比較欄位、約束、索引、RLS、有效 grants、函式、trigger、sequence、roles；額外授予 attendance_commands UPDATE 時能偵測不一致。
- 最終 `scripts/pnpm.ps1 check` 通過 lint、309 modules／0 violations、型別及回歸測試：infrastructure 83、Web 58 加環境測試 3；其他依賴測試有 Turbo 快取重用。日誌 `.artifacts/schema-structure/check.log`。
- `check`／`validate` 每次執行不使用快取的 schema:check；infrastructure 測試輸入已包含 migration 與 schemas SQL。Turbo dry-run 確認 12 份 SQL 入列，並保留依賴套件建置。
- `docs:check` 通過。Supabase CLI 2.116.0 的 db diff help 與官方概覽存在差異，因此本機差異腳本先 reset 本機、載入 schemas，再 diff；參數固定 --local。
- CLI status 回報 Docker 與 Podman 不在 PATH。Docker／PostgreSQL 17 的完整差異產生與重建流程尚未驗證；PGlite 不替代該證據。未新增 Web production build、部署、Auth 或手機驗收。
